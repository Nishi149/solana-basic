# solana-basic
wsl --install
open webstore install ubantu 24.04.5
open installed file and setup username and password 
In Vs code add an extention called 'wsl' extention and connect to server 
Now open ubantu in windows in ubantu terminal type this command 
curl --proto '=https' --tlsv1.2 -sSfL https://solana-install.solana.workers.dev | bash
check versions 
rustc --version && solana --version && anchor --version && surfpool --version && node --version && yarn --version
Clone this repo 
install live server in vs code as an extention then click on index.html and choose option as open with live server 
